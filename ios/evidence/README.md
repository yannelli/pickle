# Verification captures

Captured from the running native app on iOS simulators, September 17, 2026. Images 01–09 cover adoption, pet care, the wardrobe, friends, the system share sheet, Daily Crunch, and local party results. Images 10–11 show the final public online arena and steering against the deployed server.

Verified:

- 88 Node tests: existing browser/reminder behavior, authoritative arena rules, browser viewport/camera helpers, and shared giant-size geometry.
- 25 native model tests: persistence, rewards, daily games, invitation parsing, and arena bests.
- Native UI journeys on iPhone 17 Pro, iPhone SE (3rd generation), and iPad mini: adoption, care, wardrobe unlocks, persistence, native image sharing, solo/local party games, and live online play.
- Real WebSocket clients against both local Durable Objects and the public deployment: shared ticks, remote movement, public matchmaking, private-room sharing/isolation, forged-state rejection, disconnect cleanup, and bot refill.
- Unsigned Release archive for physical iOS devices at `../build/LittleDill.xcarchive`.

The compact iPhone care journey was rerun after correcting the test to drag inside the scroll view rather than across the fixed navigation bar. A signed Release build was subsequently installed over Wi-Fi on a physical iPhone 16 Pro Max running iOS 27.2; its running process and rendered welcome screen were verified. The signed app is at `../build/iphone/Build/Products/Release-iphoneos/LittleDill.app`. The responsiveness build was installed and launched on the physical phone; a live arena screenshot confirmed its HUD and controls fit the screen. Production-scale load testing, TestFlight, and App Store submission remain unperformed. The claimed server and branded endpoint at `arena.littledill.app` have been verified. New `care-*.png` captures show feed/pet/wash/nap on a compact iPhone; `sound-setting.png` records the persisted mute control. The old hostname forwards to the new server, verified using clients on both hostnames in a shared room.

The September 17 responsiveness update removes the 1,600 mass ceiling and tapers giant bodies to preserve a navigable garden. A live test client reached 2,002 mass in 111.3 seconds. The new native arena journey passed on the compact iPhone simulator; the native and browser clients joined the same private room. Browser inspection confirmed non-overlapping HUD bounds at 320×568 and 844×390, with the respawn card fitting the landscape viewport. `web-320.png` captures the compact browser. VisualViewport offset/keyboard cases have unit coverage; this browser verification used Chrome, not physical iPhone Safari.

Split/regroup update: 27 authoritative-rule tests include mass conservation, directional launch, four-piece limits, per-cell absorption, partial survival, automatic regroup, and shared decay/dash costs. The explicit production WebSocket smoke test earned its mass, split 1→2→4, checked matching cell IDs on a second client, and regrouped to one at 12.0 seconds. Compact native UI bounds checks cover Split, Dash, joystick, regroup status, and exit. A tap-through native split journey was not established because live opponents repeatedly ate the test player; model and live protocol checks cover the behavior. A local 24-human/96-cell simulation averaged 0.91 ms per tick on this Mac; this is not a production load test.

The subsequent split build was signed, installed, and launched on the physical iPhone. `arena-split-controls.png` shows the compact simulator controls. Desktop web Split controls were inspected live; final mobile-web split visual QA could not finish because the browser-control surface became unavailable. The earlier narrow/landscape captures predate the extra Split button. No local test fixture was deployed.

September 18 expanded-garden update: the live world is 6,000 × 4,500 (6.25× the former area), supports 64 humans per room, and fills quiet rooms to 32 participants. Native and browser share capsule artwork, palettes, circular food, and 900-unit garden regions/leaves/sector labels. UI bot badges and counts are gone; simulated opponents use unique everyday handles. `arena-expanded-native.png` shows the compact simulator against the expanded production server. Both compact live UI journeys passed, all 24 native model tests passed, and the final signed food-delta Release build was installed on the physical iPhone without forcing a relaunch.

The 88 Node tests include full 64-player/256-cell simulation, food density/growth pacing, food-grid boundary and relocation cases, delta generation/application, split/regroup, and client geometry. A local 64-connection Durable Object check admitted all clients, rejected connection 65 with 503, and freed/replaced a departing slot. With delta transport and active inputs it advanced 92 ticks during a five-second sample on this Mac; this is a local capacity check, not a distributed production load test. The 64-player/256-cell engine benchmark averaged 0.77 ms per tick (p95 1.06 ms, 50 ms budget), including snapshot serialization at its normal cadence.

Updated clients opt into ordered food deltas while legacy clients retain full snapshots. The live food integration check compares complete reconstructed inventories across legacy, modern, and mid-stream joining clients. Both local and production samples used 28% of legacy traffic; reconstructed inventories matched at every compared shared tick, including mid-stream joins. Final deployed web assets were byte-compared against the workspace, and public/private/legacy-host WebSocket checks passed. The expanded production split check again earned mass, split 1→2→4, and regrouped at 12.1 seconds. The official site's existing static assets and pet links were preserved while adding the arena navigation/hero links, dedicated feature, and FAQ through its Worker. Real local HTMLRewriter output and deployed HTTP assets were checked. Browser-control surfaces remained unavailable (`CUA_REPL_ENABLED_SURFACES is required`), so the new web artwork and website feature have not received a browser screenshot pass.

Cucumber split appearance: both clients derive the look from the authoritative piece count. Two to four pieces use a narrower fresh-green body, pale longitudinal stripes, and a curled stem when no accessory is equipped. Existing outfits and faces remain; returning to one piece restores the original brine color and silhouette, including partial survival. The 25 native tests check split, timer-expired-but-still-split, regrouped, and surviving-single states for every brine and original/crown appearances. Actual ArenaCanvas ImageRenderer captures verify the spicy-brine transition with and without a crown. `arena-cucumber-native.png` and `arena-cucumber-outfit-native.png` show the inspected spicy → cucumber → spicy sequence. All 13 browser helper tests and JavaScript syntax checks passed, and the deployed web scripts match local files. Browser screenshot verification remains unavailable because the CUA surface is missing.

The cucumber Release build was signed and installed over the network on the connected iPhone; no forced relaunch was performed.
